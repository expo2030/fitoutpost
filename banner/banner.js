/**
 * banner.js — FitOut Post marketing banner image pool
 *
 * HOW TO ADD IMAGES:
 *   1. Drop the image file into this folder (banner/)
 *   2. Add the filename to the BANNER_IMAGES array below
 *   3. No rebuild needed — the change takes effect immediately
 *
 * Image guidelines:
 *   • Landscape or square format works best (banner is ~240px tall, full width)
 *   • Portrait images also work — the center will be cropped automatically
 *   • Recommended: 1800–3000px wide, any height
 *   • Supported formats: .jpg  .jpeg  .png  .webp
 */

window.BANNER_IMAGES = [
  // Add image filenames here, e.g.:
  // "banner/my-image.jpg",
];

/* The random pick runs in the main template script — no code needed here. */
